#ifndef UNTITLED2_ADDITIONAL_H
#define UNTITLED2_ADDITIONAL_H

#include <string>
#include <vector>
using namespace std;

bool compare(const string& a, const string& b);
void print(const vector<std::string>& arr);
vector<string> generateRandomArray(int size);
vector<string> generateAlmostSortedArray(int size);
vector<string> generateAlmostUnsortedArray(int size);



#endif //UNTITLED2_ADDITIONAL_H